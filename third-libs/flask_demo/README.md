# Flask Hello World

这是一个最小的 Flask 示例应用，用于本地测试 HTTP 服务。

运行（推荐使用虚拟环境）:

Windows PowerShell:

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python app.py
```

在浏览器中打开 http://127.0.0.1:5000/ 以查看 "Hello, World!"。
